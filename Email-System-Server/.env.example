# Supabase Configuration
SUPABASE_URL=https://pyjhhoofzzldekjrwdcn.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Gemini AI Configuration (using gemini-2.0-flash model)
# Get your API key from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key

# Encryption (Must be at least 32 characters for AES-256)
ENCRYPTION_KEY=your_32_character_encryption_key_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Email Domain (for internal email delivery)
EMAIL_DOMAIN=verygoodmail.tech

# CORS (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# PhoBERT Service (optional - for advanced AI features)
PHOBERT_URL=http://localhost:8000

# ============================================================
# SMTP Configuration for SENDING emails
# For Titan Email (verygoodmail.tech domain):
# ============================================================
SMTP_HOST=smtp.titan.email
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=admin@verygoodmail.tech
SMTP_PASS="your_titan_email_password"
SMTP_FROM="Admin <admin@verygoodmail.tech>"

# ============================================================
# IMAP Configuration for RECEIVING emails (Real-time with IDLE)
# For Titan Email (verygoodmail.tech domain):
# This enables receiving emails from external sources like Gmail
# Uses IMAP IDLE for instant notifications (< 1 second delay)
# ============================================================
IMAP_HOST=imap.titan.email
IMAP_PORT=993
IMAP_SECURE=true
IMAP_USER=admin@verygoodmail.tech
IMAP_PASS="your_titan_email_password"

